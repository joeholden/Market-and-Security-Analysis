import put_call
import scrape_wsj_breadth
import OSCILLATOR

# Unfortunately if you try to run this hidden in task scheduler- it runs selenium in headless mode
# and cannot find the element to click to download the mcclellan data

# try:
#     import OSCILLATOR
# except Exception as e:
#     with open('readme.txt', 'w') as f:
#         f.write(str(e))



